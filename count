// Online C compiler to run C program online
#include<math.h>
#include <stdio.h>
#include<iostream>
using namespace std;
int count (int n)
{
    return floor(log10(n)+1);
}
int main() {
     int n;
     cout<<"enter the number";
     cin>>n;
     cout<<count(n);

    return 0;
}
